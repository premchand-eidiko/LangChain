# Frontend

The React frontend will provide authentication, chat history, document upload, and streamed agent responses.

## Phase 1

This phase contains only a minimal Vite/React entry point. The usable chat interface will be implemented after the backend contracts exist.

Install and run:

```bash
npm install
npm run dev
```
